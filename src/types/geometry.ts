interface RectangleCorners {
    x1: number | string;
    x2: number | string;
    y1: number | string;
    y2: number | string;
}


interface Dimension {
    x: number;
    y: number;
}


export type { Dimension, RectangleCorners };
